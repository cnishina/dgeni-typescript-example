/**
 * A class about Foo.
 */
export class Foo {
  /**
   * Logs something.
   * @param something a string value
   */
  logSomething(something: string) {
    console.log(something);
  }

  /**
   * Checks on something.
   * @param something a boolean value
   */
  isSomething(something: boolean) {
    return something;
  }
}